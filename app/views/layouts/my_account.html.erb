<!DOCTYPE html>
<html class="h-full bg-gray-50">
  <head>
    <title>TZU - My Account</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="<%= s3_image_url('icon.png') %>" type="image/png">
    <link rel="apple-touch-icon" href="<%= s3_image_url('icon.png') %>">
    <style>
      /* Make icons circular when displayed in the application */
      .app-icon {
        border-radius: 50%;
        overflow: hidden;
      }
    </style>

    <!-- Add Tailwind CSS CDN for immediate styling -->
    <script src="https://cdn.tailwindcss.com"></script>
    

    
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  </head>

  <body class="h-full">
    <% if notice %>
      <div class="bg-blue-50 border-l-4 border-blue-400 p-4 fixed top-4 right-4 z-50 rounded shadow-md max-w-md">
        <div class="flex">
          <div class="flex-shrink-0">
            <svg class="h-5 w-5 text-blue-400" viewBox="0 0 20 20" fill="currentColor">
              <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd" />
            </svg>
          </div>
          <div class="ml-3">
            <p class="text-sm text-blue-800"><%= notice %></p>
          </div>
        </div>
      </div>
    <% end %>
    
    <% if alert %>
      <div class="bg-red-50 border-l-4 border-red-400 p-4 fixed top-4 right-4 z-50 rounded shadow-md max-w-md">
        <div class="flex">
          <div class="flex-shrink-0">
            <svg class="h-5 w-5 text-red-400" viewBox="0 0 20 20" fill="currentColor">
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd" />
            </svg>
          </div>
          <div class="ml-3">
            <p class="text-sm text-red-800"><%= alert %></p>
          </div>
        </div>
      </div>
    <% end %>
    
    <main>
      <%= yield %>
    </main>
    

    
    <!-- Current user data for JavaScript -->
    <% if current_user %>
      <div id="current-user-data" 
           data-business-owner="<%= current_user.business_owner? %>" 
           data-has-company="<%= current_user.company.present? %>" 
           class="hidden">
      </div>
    <% end %>
  </body>
</html>
